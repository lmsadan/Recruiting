server: 
  port: 9003


spring: 
  application:  
    name: recruiting-qa #指定服务名
  datasource:  
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.1.102:3306/tensquare_qa?useSSL=false
    username: root
    password: 1234
  jpa: 
    database: MySQL
    show-sql: true

jwt:
  config:
    key: imsadan

eureka:
  client:
    service-url:
      defaultZone: http://localhost:6868/eureka/
  instance:
    prefer-ip-address: true #跨域访问

#开启熔断器
feign:
  hystrix:
    enabled: true
